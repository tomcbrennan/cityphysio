<div class="flex flex-col gap-4 p-8 bg-white border-2 border-brandBlue rounded-2xl h-full">
	<h3 class="is-h5">{{ item.title }}</h3>
	<p class="line-clamp-3 md:line-clamp-2">{{ item.preview.read_more('') }}</p>
	<a href="{{ item.link }}" class="button mt-auto">View service</a>
</div>
